name: my first ci/cd pipeline
on:
    push:
        branches: [master]
    pull_request:
        branches: [master]

jobs:
    lint:
        name: lint
        runs-on: ubuntu-latest
        steps:
            - name: checkout
              uses: actions/checkout@v6.0.2
